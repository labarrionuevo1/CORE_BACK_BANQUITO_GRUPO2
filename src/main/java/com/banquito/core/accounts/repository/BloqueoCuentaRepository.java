package com.banquito.core.accounts.repository;

import com.banquito.core.accounts.model.BloqueoCuenta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BloqueoCuentaRepository extends JpaRepository<BloqueoCuenta, Integer> {
}
